server:
  port: 8443

spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/blog?useUnicodE=true&characterEncoding=utf-8&serverTime=UTC
  jpa:
    database: mysql
#    show-sql: true
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        event:
          merge:
            entity_copy_observer: allow


